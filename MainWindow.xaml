<Window x:Class="NBAMatchPredictor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NBA AI Predictor" Height="400" Width="400" WindowStartupLocation="CenterScreen" Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="NBA Matchup" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#1E88E5"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10" HorizontalAlignment="Center">
            <TextBlock Text="Home Team:" Width="100" FontSize="16" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <ComboBox x:Name="comboHome" Width="120" FontSize="14" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10" HorizontalAlignment="Center">
            <TextBlock Text="Away Team:" Width="100" FontSize="16" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <ComboBox x:Name="comboAway" Width="120" FontSize="14" Padding="5"/>
        </StackPanel>

        <Button Grid.Row="3" x:Name="btnPredict" Content="Calculate Win Probability" Height="45" Width="220" Margin="0,20,0,0" 
                Background="#43A047" Foreground="White" FontSize="16" FontWeight="Bold" Cursor="Hand" Click="btnPredict_Click"/>

        <TextBlock Grid.Row="4" x:Name="txtResult" Text="Select teams and run analysis." FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#333" TextAlignment="Center"/>
    </Grid>
</Window>